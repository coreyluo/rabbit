<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bazinga.replay.dao.AdsReturnSealDataDAO">

    <resultMap id="BaseResultMap" type="com.bazinga.replay.model.AdsReturnSealData">
        <id column="id" jdbcType="INTEGER"  property="id" />
        <result column="stock_code" jdbcType="VARCHAR" property="stockCode"/>
        <result column="date_trade" jdbcType="VARCHAR" property="dateTrade"/>
        <result column="plank_time" jdbcType="VARCHAR" property="plankTime"/>
        <result column="return_count" jdbcType="INTEGER" property="returnCount"/>
        <result column="unsealed_time" jdbcType="VARCHAR" property="unsealedTime"/>
        <result column="unsealed_diff" jdbcType="INTEGER" property="unsealedDiff"/>
    </resultMap>


    <sql id="Base_Column_List">
        id,
            stock_code,
            date_trade,
            plank_time,
            return_count,
            unsealed_time,
            unsealed_diff
        </sql>


    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from ads_return_seal_data
        where id = #{id}
    </select>


    <insert id="insert" parameterType="com.bazinga.replay.model.AdsReturnSealData" useGeneratedKeys="true" keyProperty="id">
        insert into ads_return_seal_data (<include refid="Base_Column_List" />)
        values(
            #{id, jdbcType=INTEGER},
            #{stockCode, jdbcType=VARCHAR},
            #{dateTrade, jdbcType=VARCHAR},
            #{plankTime, jdbcType=VARCHAR},
            #{returnCount, jdbcType=INTEGER},
            #{unsealedTime, jdbcType=VARCHAR},
            #{unsealedDiff, jdbcType=INTEGER},
            )
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.bazinga.replay.model.AdsReturnSealData">
        update ads_return_seal_data
        <set>
            <if test="stockCode != null">
                stock_code = #{stockCode, jdbcType=VARCHAR},
            </if>
            <if test="dateTrade != null">
                date_trade = #{dateTrade, jdbcType=VARCHAR},
            </if>
            <if test="plankTime != null">
                plank_time = #{plankTime, jdbcType=VARCHAR},
            </if>
            <if test="returnCount != null">
                return_count = #{returnCount, jdbcType=INTEGER},
            </if>
            <if test="unsealedTime != null">
                unsealed_time = #{unsealedTime, jdbcType=VARCHAR},
            </if>
            <if test="unsealedDiff != null">
                unsealed_diff = #{unsealedDiff, jdbcType=INTEGER}
            </if>
        </set>
        where id = #{id} limit 1
    </update>

    <sql id="Equal_Where_List">
        <if test="stockCode != null">
            and stock_code = #{stockCode, jdbcType=VARCHAR}
        </if>
        <if test="dateTrade != null">
            and date_trade = #{dateTrade, jdbcType=VARCHAR}
        </if>
        <if test="plankTime != null">
            and plank_time = #{plankTime, jdbcType=VARCHAR}
        </if>
        <if test="returnCount != null">
            and return_count = #{returnCount, jdbcType=INTEGER}
        </if>
        <if test="unsealedTime != null">
            and unsealed_time = #{unsealedTime, jdbcType=VARCHAR}
        </if>
        <if test="unsealedDiff != null">
            and unsealed_diff = #{unsealedDiff, jdbcType=INTEGER}
        </if>
    </sql>

    <sql id="Range_Where_List">
    </sql>

    <sql id="GtLastMaxId_Where">
        <if test="lastMaxId != null and lastMaxId > 0">
            and id > #{lastMaxId}
        </if>
    </sql>

    <sql id="Order_By">
        <if test="sorts != null and sorts.size > 0">
            ORDER BY
            <foreach collection="sorts" item="sortItem" separator=",">
                ${sortItem.column} ${sortItem.type.message}
            </foreach>
        </if>
    </sql>

    <select id="selectByCondition" parameterType="com.bazinga.replay.query.AdsReturnSealDataQuery" resultMap="BaseResultMap">
        select
            <include refid="Base_Column_List"/>
        from ads_return_seal_data
        <where>
            <include refid="Equal_Where_List"/>
            <include refid="Range_Where_List"/>
            <include refid="GtLastMaxId_Where" />
        </where>
        <include refid="Order_By"/>
        <if test="limit > 0">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="countByCondition" parameterType="com.bazinga.replay.query.AdsReturnSealDataQuery" resultType="java.lang.Integer">
        select count(1)
        from ads_return_seal_data
        <where>
            <include refid="Equal_Where_List"/>
            <include refid="Range_Where_List"/>
        </where>
    </select>


</mapper>